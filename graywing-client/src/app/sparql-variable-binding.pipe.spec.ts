import { SparqlVariableBindingPipe } from './sparql-variable-binding.pipe';

describe('SparqlVariableBindingPipe', () => {
  it('create an instance', () => {
    const pipe = new SparqlVariableBindingPipe();
    expect(pipe).toBeTruthy();
  });
});
